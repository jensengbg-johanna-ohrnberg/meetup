import { shallowMount } from '@vue/test-utils'
import EventCard from '@/components/EventCard.vue'

describe('EventCard', () => {
    /*let wrapper
    beforeEach(() => {
        wrapper = shallowMount(EventCard);
    })*/
    it('should show name of the event', () => {

    })

    it('should show date of the event', () => {

    })

    it('should show location of the event', () => {

    })
})